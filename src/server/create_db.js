// ======================================================
// App / Database / Create
// ======================================================

/**
 * Create all the tables.
 * @param {*} db Empty SQLite Database Object.
 */
const createDatabaseTables = db => {
  return db;
};
